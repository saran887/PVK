name: pkv2
description: "A Flutter distribution management app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.5.1
  firebase_storage: ^12.4.0
  
  # State Management
  flutter_riverpod: ^2.6.1
  riverpod: ^2.6.1
  
  # Navigation
  go_router: ^14.8.1
  
  # UI
  image_picker: ^1.2.1
  intl: ^0.19.0
  http: ^1.2.0
  excel: ^4.0.6
  pdf: ^3.11.1
  printing: ^5.13.3
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  whatsapp_share2: ^2.0.3
  permission_handler: ^11.3.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
